(function webpackUniversalModuleDefinition(root, factory) {
	if(typeof exports === 'object' && typeof module === 'object')
		module.exports = factory(require("yeelight2"), require("color-convert"));
	else if(typeof define === 'function' && define.amd)
		define(["yeelight2", "color-convert"], factory);
	else if(typeof exports === 'object')
		exports["yeelight-compat-hue"] = factory(require("yeelight2"), require("color-convert"));
	else
		root["yeelight-compat-hue"] = factory(root["yeelight2"], root["color-convert"]);
})(typeof self !== 'undefined' ? self : this, function(__WEBPACK_EXTERNAL_MODULE_0__, __WEBPACK_EXTERNAL_MODULE_1__) {
return /******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId]) {
/******/ 			return installedModules[moduleId].exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			i: moduleId,
/******/ 			l: false,
/******/ 			exports: {}
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.l = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// define getter function for harmony exports
/******/ 	__webpack_require__.d = function(exports, name, getter) {
/******/ 		if(!__webpack_require__.o(exports, name)) {
/******/ 			Object.defineProperty(exports, name, {
/******/ 				configurable: false,
/******/ 				enumerable: true,
/******/ 				get: getter
/******/ 			});
/******/ 		}
/******/ 	};
/******/
/******/ 	// getDefaultExport function for compatibility with non-harmony modules
/******/ 	__webpack_require__.n = function(module) {
/******/ 		var getter = module && module.__esModule ?
/******/ 			function getDefault() { return module['default']; } :
/******/ 			function getModuleExports() { return module; };
/******/ 		__webpack_require__.d(getter, 'a', getter);
/******/ 		return getter;
/******/ 	};
/******/
/******/ 	// Object.prototype.hasOwnProperty.call
/******/ 	__webpack_require__.o = function(object, property) { return Object.prototype.hasOwnProperty.call(object, property); };
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "";
/******/
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(__webpack_require__.s = 3);
/******/ })
/************************************************************************/
/******/ ([
/* 0 */
/***/ (function(module, exports) {

module.exports = __WEBPACK_EXTERNAL_MODULE_0__;

/***/ }),
/* 1 */
/***/ (function(module, exports) {

module.exports = __WEBPACK_EXTERNAL_MODULE_1__;

/***/ }),
/* 2 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
    value: true
});

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

exports.rgbIntToHex = rgbIntToHex;
exports.colorTemperatureToRgb = colorTemperatureToRgb;
exports.clamp = clamp;
exports.hexToRgbInt = hexToRgbInt;
exports.colorTemperatureToRgbInt = colorTemperatureToRgbInt;
exports.normalize = normalize;
exports.sanitizeState = sanitizeState;

var _colorConvert = __webpack_require__(1);

var _colorConvert2 = _interopRequireDefault(_colorConvert);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

if (!String.prototype.padStart) {
    String.prototype.padStart = function padStart(targetLength, padString) {
        targetLength = targetLength >> 0;
        padString = String(padString || ' ');
        if (this.length > targetLength) {
            return String(this);
        } else {
            targetLength = targetLength - this.length;
            if (targetLength > padString.length) {
                padString += padString.repeat(targetLength / padString.length);
            }
            return padString.slice(0, targetLength) + String(this);
        }
    };
}

function rgbIntToHex(number) {
    return '#' + (number >>> 0).toString(16).slice(-6).padStart(6, '0');
}

function colorTemperatureToRgb(kelvin) {
    var temp = kelvin / 100;
    var red, green, blue;

    if (temp <= 66) {
        red = 255;
        green = temp;
        green = 99.4708025861 * Math.log(green) - 161.1195681661;

        if (temp <= 19) {
            blue = 0;
        } else {
            blue = temp - 10;
            blue = 138.5177312231 * Math.log(blue) - 305.0447927307;
        }
    } else {
        red = temp - 60;
        red = 329.698727446 * Math.pow(red, -0.1332047592);

        green = temp - 60;
        green = 288.1221695283 * Math.pow(green, -0.0755148492);

        blue = 255;
    }

    return {
        red: clamp(red, 0, 255),
        green: clamp(green, 0, 255),
        blue: clamp(blue, 0, 255)
    };
}

function clamp(x, min, max) {
    if (x < min) {
        return min;
    }
    if (x > max) {
        return max;
    }

    return x;
}

function hexToRgbInt(hex) {
    return parseInt('0x' + hex.replace('#', ''), 16);
}

function colorTemperatureToRgbInt(ct) {
    var _colorTemperatureToRg = colorTemperatureToRgb(ct),
        red = _colorTemperatureToRg.red,
        green = _colorTemperatureToRg.green,
        blue = _colorTemperatureToRg.blue;

    return hexToRgbInt('#' + _colorConvert2.default.rgb.hex(red, green, blue));
}

function normalize(value) {
    var currentMax = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 100;
    var newMax = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : 255;

    return Math.round(parseInt(value, 10) / currentMax * newMax);
}

function sanitizeState(state) {
    var bright = state.bright,
        power = state.power,
        name = state.name,
        color_mode = state.color_mode;

    var colorModeMap = {
        1: 'rgb',
        2: 'ct',
        3: 'hs'
    };
    var result = {
        state: {
            on: state.power !== 'off',
            bri: normalize(bright),
            colormode: colorModeMap[color_mode]
        },
        name: name,
        raw: state
    };
    if (state.color_mode === '1') {
        var hex = rgbIntToHex(state.rgb);
        result.state = _extends({}, result.state, {
            hex: hex,
            hue: normalize(_colorConvert2.default.hex.hsv(hex)[0], 359, 65535),
            sat: normalize(_colorConvert2.default.hex.hsv(hex)[1])
        });
    } else if (state.color_mode === '2') {
        var _colorTemperatureToRg2 = colorTemperatureToRgb(state.ct),
            red = _colorTemperatureToRg2.red,
            green = _colorTemperatureToRg2.green,
            blue = _colorTemperatureToRg2.blue;

        result.state = _extends({}, result.state, {
            ct: parseInt(state.ct, 10),
            hex: '#' + _colorConvert2.default.rgb.hex(red, green, blue),
            hue: normalize(_colorConvert2.default.rgb.hsv(red, green, blue)[0], 359, 65535),
            sat: normalize(_colorConvert2.default.rgb.hsv(red, green, blue)[1])
        });
    } else if (state.color_mode === '3') {
        var _hex = _colorConvert2.default.hsv.hex(state.hue, state.sat, state.bright);
        result.state = _extends({}, result.state, {
            hex: '#' + _hex,
            hue: normalize(state.hue, 359, 65535),
            sat: normalize(state.sat)
        });
    }
    return result;
}

/***/ }),
/* 3 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _YeeLightNodeOut = __webpack_require__(4);

var _YeeLightNodeOut2 = _interopRequireDefault(_YeeLightNodeOut);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

module.exports = function (RED) {
    RED.nodes.registerType('yeelight-compat-hue-out', (0, _YeeLightNodeOut2.default)(RED));
};

/***/ }),
/* 4 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
    value: true
});

var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; }; // https://www.yeelight.com/download/Yeelight_Inter-Operation_Spec.pdf
// https://github.com/song940/node-yeelight/blob/master/index.js

exports.default = YeeLightNodeOut;

var _yeelight = __webpack_require__(0);

var _yeelight2 = _interopRequireDefault(_yeelight);

var _colorConvert = __webpack_require__(1);

var _colorConvert2 = _interopRequireDefault(_colorConvert);

var _utils = __webpack_require__(2);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function YeeLightNodeOut(RED) {
    return function (config) {
        var node = this;

        var onInput = function onInput(msg) {
            if (typeof msg.payload === 'string') {
                try {
                    msg.payload = JSON.parse(msg.payload);
                } catch (e) {
                    return node.error('Yeelight: Error during payload parsing\n' + e + '\n' + msg.payload);
                }
            }

            if (_typeof(msg.payload) !== 'object') {
                return node.error('Yeelight: Invalid payload\n' + msg.payload);
            }

            var _msg$payload = msg.payload,
                on = _msg$payload.on,
                hex = _msg$payload.hex,
                bri = _msg$payload.bri,
                hue = _msg$payload.hue,
                sat = _msg$payload.sat,
                _msg$payload$duration = _msg$payload.duration,
                duration = _msg$payload$duration === undefined ? 500 : _msg$payload$duration,
                ct = _msg$payload.ct;


            if (on === false) {
                return node.serverConfig.yeelight.set_power(on, null, duration);
            }

            node.serverConfig.yeelight.sync().then(function (state) {
                var colorValue = void 0;
                var colorMode = void 0;
                var currentState = (0, _utils.sanitizeState)(state).state;
                var briToTurnTo = (0, _utils.clamp)((0, _utils.normalize)(bri || currentState.bri, 255, 100), 1, 100);

                if (typeof ct !== 'undefined') {
                    colorMode = 2;
                    colorValue = ct;
                } else if (typeof hex !== 'undefined') {
                    colorMode = 1;
                    colorValue = (0, _utils.hexToRgbInt)(hex);
                    // if no bri was specified, calculate from hex value
                    briToTurnTo = bri ? briToTurnTo : (0, _utils.clamp)(_colorConvert2.default.hex.hsv(hex)[2], 1, 100);
                } else if (typeof hue !== 'undefined' || typeof sat !== 'undefined' || typeof bri !== 'undefined') {
                    colorMode = 1;
                    colorValue = (0, _utils.hexToRgbInt)(_colorConvert2.default.hsv.hex((0, _utils.normalize)(hue || currentState.hue, 65535, 359), (0, _utils.normalize)(sat || currentState.sat, 255, 100), briToTurnTo));
                } else if (on) {
                    return node.serverConfig.yeelight.set_power(on, null, duration);
                }

                return node.serverConfig.yeelight.set_scene('cf', 1, // don't repeat
                1, // keep in end-state
                duration + ', ' + colorMode + ', ' + colorValue + ', ' + briToTurnTo).catch(function (e) {
                    if (e.code === -5000) {
                        return node.error('Yeelight "general error (code -5000)". Payload might be invalid.');
                    }
                    throw e;
                });
            }).catch(function (e) {
                if (e.message === 'timeout') {
                    e.code = 'timeout';
                    e.message = 'Local timeout in "Yeelight.command" execution';
                }
                node.log('An error occured while syncing or setting a new value');
                console.error(e);
                node.status({
                    fill: 'red',
                    shape: 'ring',
                    text: 'Command send error: ' + e.code
                });
            });
        };

        (function init() {
            RED.nodes.createNode(node, config);
            node.serverConfig = RED.nodes.getNode(config.server);

            if (!node.serverConfig || !node.serverConfig.hostname) {
                node.status({ fill: 'red', shape: 'ring', text: 'Hostname not set' });
                return;
            }

            node.serverConfig.registerClientNode(node);

            node.on('input', onInput);

            node.on('close', function () {
                if (node.serverConfig) {
                    node.serverConfig.deregisterClientNode(node);
                }
            });
        })();
    };
}

/***/ })
/******/ ]);
});